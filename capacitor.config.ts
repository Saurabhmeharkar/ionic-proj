import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'recilica-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
